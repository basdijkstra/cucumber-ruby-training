Given('a feature file with steps without step definitions') do
end

When('I generate step definitions for the steps') do
end

Then('the steps will be implemented by the generated step definitions') do
end